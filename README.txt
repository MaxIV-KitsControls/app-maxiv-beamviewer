A Lima camera viewer for YAG screens

Starting:

   $ python src/limayag.py lima/limaccd/1

Needs to be pointed to a limaccd instance which is running the "YAG" plugin.

This application uses the "PyQtGraph" module (http://www.pyqtgraph.org/) and so depends on it being installed.
